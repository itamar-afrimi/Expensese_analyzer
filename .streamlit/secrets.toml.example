# Copy this to secrets.toml for local development,
# or paste into Streamlit Cloud "Secrets" settings for deployment.
# NEVER commit secrets.toml to git.

[app]
password = "your-family-password"

[openai]
api_key = "sk-..."
